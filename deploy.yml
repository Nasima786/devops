- name: Deploy Web Server
  hosts: localhost
  become: yes

  tasks:
    - name: Replace index page
      copy:
        content: "<h1>Hello from DevOps Project 🚀</h1>"
        dest: /var/www/html/index.html
